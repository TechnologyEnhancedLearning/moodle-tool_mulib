{{!
This file is part of Additional tools library for Moodle™.

@copyright   2025 Petr Skoda
@license     https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later

}}

{{#disabled}}
<span title="{{title}}" class="dimmed_text {{class}}">{{{icon}}}</span>
{{/disabled}}
{{^disabled}}
{{#legacyformtest}}
<a href="{{formurl}}" title="{{title}}" class="{{class}}">{{{icon}}}</a>
{{/legacyformtest}}
{{^legacyformtest}}
<noscript><a href="{{formurl}}">{{title}}</a></noscript>
<a id="dialog-form-action-{{uniqid}}" href="#" title="{{title}}" class="{{class}}" data-dialog-size="{{dialogsize}}"
  data-form-url="{{formurl}}" data-dialog-name="{{dialogname}}" data-after-submit="{{aftersubmit}}"
>{{{icon}}}</a>

{{#js}}
require([
    'tool_mulib/dialog_form',
], function(DialogForm) {
    let action = document.getElementById('dialog-form-action-' + "{{uniqid}}");

    action.onclick = function() {
        new DialogForm({
            modalConfig: {title: action.dataset.dialogName},
            triggerElement: action,
            formUrl: action.dataset.formUrl,
            afterSubmit: action.dataset.afterSubmit,
            size: action.dataset.dialogSize,
        });
    };
});
{{/js}}
{{/legacyformtest}}
{{/disabled}}
